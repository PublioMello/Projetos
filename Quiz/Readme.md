# Quiz de Múltipla Escolha em React

Quiz responsivo de JavaScript, criado com React e Vite. O jogador escolhe uma alternativa por pergunta, vê a pontuação final e pode iniciar uma nova partida.

## Executar localmente

```bash
npm install
npm run dev
```

Para validar uma versão de produção:

```bash
npm run build
```
